export const mainAudio = document.getElementById(
  "gameMusic"
) as HTMLAudioElement;

export const winAudio = document.getElementById(
  "winMusic"
) as HTMLAudioElement;

export const loseAudio = document.getElementById(
  "loseMusic"
) as HTMLAudioElement;
